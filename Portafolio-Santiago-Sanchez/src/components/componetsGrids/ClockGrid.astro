---

---

<script>
  import { analogoNumeros } from "./ClockDigital";

  function actualizarHora() {
    const ahora = new Date();
    const horas = ahora.getHours();
    const minutos = ahora.getMinutes();
    const segundos = ahora.getSeconds();

    const formatoHoras = analogoNumeros(horas);
    const formatoMinutos = analogoNumeros(minutos);
    const formatoSegundos = analogoNumeros(segundos);

    const horaPersonalizada = `${formatoHoras}\n---------------------\n\n${formatoMinutos}\n---------------------\n\n${formatoSegundos}`;

    // Obtiene el elemento y verifica si no es null
    const horaElemento = document.getElementById("hora");
    if (horaElemento) {
      horaElemento.innerText = horaPersonalizada;
    }
  }

  // Actualiza la hora cada segundo
  setInterval(actualizarHora, 1000);
  actualizarHora();
</script>

<div>
  <h1 id="hora"></h1>
</div>

<style>
  #hora {
    font-family: "Lucida Console", "Monaco", monospace;
    padding: 1vh 1vw; /* Reducido el padding */
    margin: 0 auto;
    font-size: clamp(
      0.6rem,
      1.8vh,
      1.8vw
    ); /* Reducido el tamaño de la fuente */
    text-align: center;
    border-radius: 20px;
    display: block;
    width: 90%; /* Reducido ligeramente del 100% */
    color: #00ff00;
    background: #000000f8;
    white-space: pre-wrap;
    letter-spacing: 0;
    font-weight: 900;
    text-shadow: 0 0 10px #00ff00;
    line-height: 1.3; /* Reducido el interlineado */
    box-sizing: border-box;
    overflow-wrap: break-word;
    max-width: 100%; /* Asegura que no se exceda del ancho del padre */
    overflow: hidden; /* Oculta el contenido que se desborda */
  }
</style>
