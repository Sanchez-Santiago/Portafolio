<script>
  import { analogoNumeros } from "./ClockDigital";
  function actualizarHora() {
    const ahora = new Date();
    const horas = ahora.getHours();
    const minutos = ahora.getMinutes();
    const segundos = ahora.getSeconds();
    const formatoHoras = analogoNumeros(horas);
    const formatoMinutos = analogoNumeros(minutos);
    const formatoSegundos = analogoNumeros(segundos);
    const horaPersonalizada = `${formatoHoras}\n---------------------\n\n${formatoMinutos}\n---------------------\n\n${formatoSegundos}`;
    // Obtiene el elemento y verifica si no es null
    const horaElemento = document.getElementById("hora");
    if (horaElemento) {
      horaElemento.innerText = horaPersonalizada;
    }
  }
  // Actualiza la hora cada segundo
  setInterval(actualizarHora, 1000);
  actualizarHora();
</script>
<div class="contenedor-clock">
  <pre id="hora"></pre>
</div>
<style>
  .contenedor-clock {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    padding: 0;
    margin: 0;
  }

  #hora {
    font-family: "Lucida Console", "Monaco", monospace;
    padding: 0.5vh 0.5vw;
    margin: 0 auto;
    font-size: 0.7rem; /* Tamaño más pequeño para adaptarse */
    text-align: center;
    border-radius: 10px;
    display: block;
    width: 95%;
    max-width: 95%;
    color: #00ff00;
    background: #000000f8;
    white-space: pre-wrap;
    font-weight: 900;
    text-shadow: 0 0 5px #00ff00;
    line-height: 1.2;
    box-sizing: border-box;
    overflow-wrap: break-word;
    letter-spacing: -0.5px; /* Reducir espacio entre letras */
    transform: scale(0.95); /* Ligera reducción para asegurar que encaje */
  }

  /* Media query para diferentes tamaños */
  @media (min-width: 768px) and (max-width: 1024px) {
    #hora {
      font-size: 0.65rem;
      transform: scale(0.9);
    }
  }

  @media (min-width: 1025px) {
    #hora {
      font-size: 0.8rem;
    }
  }

  @media (max-width: 767px) {
    #hora {
      font-size: 0.6rem;
      padding: 0.4vh 0.4vw;
      transform: scale(0.85);
      letter-spacing: -1px;
    }
  }
</style>
